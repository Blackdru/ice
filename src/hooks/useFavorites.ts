import {useFavoritesContext} from '../context/FavoritesContext';

/**
 * Convenience re-export of useFavoritesContext.
 * Provides a shorter import path for components.
 */
export const useFavorites = useFavoritesContext;
